{
  "name": "france-article-map",
  "version": "1.0.0",
  "description": "Map of France with article links pinned by location, date and radius, filterable by date",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node scripts/build.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "express": "^4.19.2"
  }
}
